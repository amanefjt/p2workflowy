# SPEC: 深読モード（Deep Reading Mode）

- **ステータス**: 仕様確定（一部検証待ち、§11 参照）
- **作成日**: 2026-06-10
- **作成方法**: 藤田氏へのインタビュー（AskUserQuestion 6 ラウンド）に基づく
- **対象リポジトリ**: p2workflowy（学術論文・専門書籍 → Workflowy 向け日本語アウトライン変換ツール）

---

## 1. 背景とゴール

### 1.1 背景

p2workflowy の現行出力は「構造認識＋対訳＋レジュメ」であり、テクストに**書かれていること**を日本語アウトラインに写像する。本機能はその先、すなわち **「人文系の論文・書籍を『読めた』とは何か」** に踏み込む。

人文系テクストの「読めた」は、要約の把握では完結しない。本仕様は以下の 3 つの理解を「読めた」の構成要素と定義し、それぞれを機能化する。

1. **問いの構造がわかった** — 書かれた事実の向こう側にある大きな問い・関心がわかると、その論文の使い方・位置付けができる
2. **どう説得されたかがわかった** — 論証の要所（転換・跳躍・伏線回収）が見えると、ヨミの緩急がつけられる
3. **翻訳が行った解釈がわかった** — 訳文の解釈的選択が開示されると、訳文越しでも原文の多義性に触れられる

### 1.2 ビジネスゴール

- 一般公開ユーザー（Web 版）の読書体験を「翻訳の受領」から「深い読解の支援」へ拡張する
- 慣習的な読解支援（要約・問い生成・用語集）の再生産ではなく、AI でこそ可能になる理解支援を提供する

### 1.3 成功指標

- [ ] テスト資産（AL / NST / Techno 論文）で、生成された「問いの構造」が著者（藤田氏）の専門的読解と一致または有益な差異を示す
- [ ] 深読モード ON 時の追加コスト・処理時間が通常変換比 **+30% 以内**（§9）
- [ ] 深読モード ON/OFF で**通常翻訳の品質が変化しない**こと（§10 検証 2）

---

## 2. コンセプト: 二層の問いスキーマ

本機能の核。**「人文学は対象の個別性の問いと理論的な問いを同時に解く」** という人文学観（藤田氏の定式）に基づき、論文の問い・主張を二層 5 スロットで構造化する。

| スロット | 人類学の場合（例） | 汎用語彙（フォールバック） |
|---|---|---|
| 1. 課題 | 民族誌的課題 | 対象の課題 |
| 2-1. 個別の問い | 民族誌的問い | 対象への問い |
| 2-2. 理論の問い | 人類学的問い | 理論的問い |
| 3-1. 個別の主張 | 民族誌的主張 | 対象についての主張 |
| 3-2. 理論の主張 | 人類学的主張 | 理論的主張 |

具体例（イメージ）:

```
🌊 この論文の問いの構造
├─ 民族誌的課題: イヌイトの狩猟実践の記述
├─ 民族誌的問い: なぜ狩人は動物を「人」と呼ぶのか
├─ 人類学的問い: 自然/社会の二分法は普遍的か
├─ 民族誌的主張: 狩猟は交渉である
└─ 人類学的主張: 二分法は近代西洋の局所的構築物
```

**設計上の位置付け — 規制的理想としてのテンプレート**: この 5 スロットは全論文に強制適用しない。純理論論文・レビュー論文など対象層を持たないテクストでは単層に縮退し、その旨を明示する（§8 E-1）。スキーマの定式自体の妥当性はプロンプト実験で検証する（§11 U-1）。

**語彙の動的化**: スロットのラベルは Web UI / CLI の「専門分野（expertise）」入力に連動して LLM が分野に適した語彙を生成する（人類学→民族誌的/人類学的、歴史学→史料的/歴史学的 など）。expertise 未入力または分野判定不能の場合は汎用語彙にフォールバックする。これは既存の「書籍モード expertise 動的化」と同一の設計パターン。

---

## 3. スコープ

### 3.1 スコープ IN（このバージョンで実装する）

- **F1: 問いの構造レイヤー** — 二層問いスキーマ＋背後の問い＋介入関係（レジュメの 1・2 を置換）
- **F2: 論証地図** — 要所のみの論証注釈（転換点・跳躍・伏線回収）＋暗黙のテンポ信号
- **F3: 解釈開示ノート** — 解釈分岐・翻訳不可能性・訳語選択の開示（厳選数件）
- **F4: UI / CLI** — 「深読モード」一括トグル（Web）、`--deep` フラグ（CLI）
- **F5: パイプライン統合** — 独立した深読解析フェーズの追加＋Phase 4 プロンプト 2 ルート化
- 対象は**論文モードのみ**、**英語ソースのみ**（現行と同じ）
- 通常エクスポートと `ronbunnihongo` エクスポートの両方で出力（注釈はすべてレジュメ＋日本語本文側に載るため、両モードで自然に機能する）

### 3.2 スコープ OUT（意図的に含めない）

- **書籍モード対応** — 章横断の反響・概念追跡の設計課題が大きいため、論文モードでの検証後に別途設計する
- **対話型 UI（変換後チャット）** — 静的アウトライン埋込で完結させる。Workflowy 上で読み終わる体験を崩さない
- **多言語ソース（ES/ZH/KO）** — 既存の延期判断（2026-05-28）を維持
- **以下の慣習的読解支援機能** — インタビューで検討の上、不採用: セクション要約レイヤー（既存レジュメと重複）、批判的読解の問い生成、概念グロッサリー自動生成、概念の伝記、訳語ゆれ自動統一、構造整合性チェッカー、変換品質レポート
- **全段落への動きラベル付与** — ノイズになるため要所のみ（F2）
- **デフォルト有効化** — 深読モードは常にオプトイン

### 3.3 してはいけないこと（制約）

- **根拠なき推論の出力禁止**: 本文上の手がかり（セクション参照・引用パターン等）を示せない推論ノードは出力しない（F1-4）
- **通常ルートの翻訳プロンプトを変更しない**: 深読 OFF 時の Phase 4 プロンプトは現行とバイト同一を維持する（F5-3）
- **深読解析の失敗で本体変換を道連れにしない**: 深読フェーズが失敗しても通常変換は完走する（F5-5）
- **原文（English text ブロック）ノードを汚さない**: 深読の注釈はレジュメと日本語本文ブロックにのみ載せる
- **用語集 CSV と矛盾しない**: 解釈開示ノートが用語集の登録訳語を覆す提案をしない（用語集優先）

---

## 4. ユーザー・ユースケース

**ユーザー像**: 一般公開 Web 版（p2workflowy.pages.dev / HF Spaces）の利用者。人文系の論文を読む研究者・大学院生・学生。分野は人類学に限らない。自分の Gemini API キーを使用（コストは自己負担）。

**ユースケース**:

- UC-1: ゼミで人文系論文を発表するとき、論文の「問いの構造」を最初に掴んでから精読箇所を決めたい
- UC-2: 自分の研究との関係を考えるとき、論文が参加している学術的会話と介入のポイントを知り、引用・位置付けの仕方を判断したい
- UC-3: 長い論文を限られた時間で読むとき、論証の要所だけ精読し、残りは流し読みしたい（ヨミの緩急）
- UC-4: 訳文で読んでいて解釈が怪しい箇所に出会ったとき、原文の多義性と訳の選択理由を知りたい
- UC-5: コストを抑えたいとき、深読モードを OFF にして従来どおりの変換だけを使いたい

---

## 5. 機能要求

記述形式: 「[条件]のとき、[主語]は[動作]する」

### F1: 問いの構造レイヤー

- **F1-1**: 深読モードが有効のとき、システムはレジュメの「1. リサーチ・クエスチョン」「2. 核心的主張」を以下の 2 ノードに**置換**する（「3. 各セクションの展開」は現状維持）:
  - 「1. 問いの構造」— 二層 5 スロット（§2）
  - 「2. 背後の問いと介入」— 背後の大きな問い、応答する相手・立場、継承する系譜、ずらしのポイント
- **F1-2**: expertise が入力されているとき、システムはスロットのラベルを分野に適した語彙で生成する。未入力・判定不能のとき、汎用語彙（§2 表）にフォールバックする
- **F1-3**: 「背後の大きな問い」を推論するとき、LLM は「理論的問い」と「経験的パズル」を統合した観点（対象の個別性と理論を同時に解くという人文学観）から、本文に明示されていない著者の根本的関心を推誇してよい
- **F1-4**: 推論を含むノードを出力するとき、システムは必ず本文上の根拠（セクション参照・引用箇所・引用パターン）を併記し、「（本文に明示なし、〜からの推論）」のラベルで本文記載と区別する。根拠を示せない推論は出力しない
- **F1-5**: テクストが対象層を持たない（純理論論文等）とき、システムは二層スキーマを単層に縮退させ、「この論文は対象層を持たない」旨を明示する

**受け入れ基準**:
- [ ] AL / NST 論文で深読モード実行時、レジュメ 1・2 が問いの構造＋背後の問いに置換されている
- [ ] 人類学論文＋expertise「人類学」で「民族誌的/人類学的」系の語彙が使われる
- [ ] expertise 空欄で汎用語彙（対象の/理論的）が使われる
- [ ] すべての推論ノードに根拠参照と推論ラベルが付いている
- [ ] 純理論論文（対象層なし）を入力すると単層に縮退し、その旨が明示される
- [ ] 深読モード OFF のとき、レジュメは現行出力と同一形式

### F2: 論証地図（要所マーク＋テンポ信号）

- **F2-1**: 深読モードが有効のとき、システムは論証上の要所（事例→理論の跳躍、転換点、譲歩、伏線回収、ヴィネットによる説得など「動きのある箇所」）を**論文あたり 5〜10 箇所**特定する。短い論文（本文 10 段落未満）では 3 箇所以下に抑える。上限は本文段落総数の約 10% を超えない
- **F2-2**: 要所を出力するとき、システムは**日本語本文ブロック**の該当訳段落に対して、(a) 段落末尾にインラインマーカー `［◆精読］` を付加し、(b) 直下に詳細子ノードを挿入する:

  ```
  - 日本語本文
  - The multi-sited imaginary 3: freedom
  	- この経験から著者が得た洞察は…である。［◆精読］
  		- ⚙️ ここで個人的経験から方法論的主張へ跳躍。
  		  冒頭の「自由の皮肉」が ここで回収される（→ Introduction: 1995）
  	- （マーカーなしの段落 ＝ 流し読み可、という暗黙のテンポ信号）
  ```

- **F2-3**: 要所が別セクションと呼応する（応答・伏線回収・再定義）とき、システムは子ノード内に `（→ セクションタイトル）` 形式で参照を記す。参照先は出力に実在するセクションタイトル文字列のみ使用する（Workflowy プレーンテキストにはアンカーがないため、タイトル文字列照合で読者が辿る）
- **F2-4**: 参照先が `[Unlabeled Section]` のとき、システムは `（→ 冒頭部）` と表記する
- **F2-5**: 原文（English text）ブロックには一切マーカー・子ノードを追加しない

**受け入れ基準**:
- [ ] AL / NST 論文で要所マークが 5〜10 箇所生成され、すべて訳段落末尾＋子ノードの形式である
- [ ] マークされた要所が訳段落と正しく対応している（段落の取り違えがない）
- [ ] セクション間参照の参照先タイトルがすべて出力中に実在する
- [ ] English text ブロックは深読 ON/OFF でバイト同一
- [ ] `.md` 出力でも同等の注釈が（blockquote 等の Markdown 慣用形式で）出力される

### F3: 解釈開示ノート

- **F3-1**: 深読モードが有効のとき、Phase 4 の翻訳 LLM は翻訳中に「解釈が分かれる」と判断した箇所にフラグを立て、種別・別読み・理由を構造化して返す。種別は 3 つ:
  - `分岐` — 構文・語義の多義性で複数の読みが成立する（別読みと、どちらを選んだ根拠を記す）
  - `不可能` — 原語の響き・概念負荷が訳出不能（原語併記と、なぜ訳せないかを記す）
  - `訳注` — 思い切った訳語選択をした（なぜこう訳したかを記す）
- **F3-2**: フラグは**論文あたり数件（最大 5 件、1 セクション 1 件目安）**に厳選する。「見過ごすと誤読する箇所」に限定し、信号の価値を保つ
- **F3-3**: 解釈開示ノートを出力するとき、システムは日本語本文ブロックの該当訳段落直下に子ノードとして挿入する:

  ```
  	- 住まうことは世界制作に先立つ。
  		- ⚖️ 分岐: 「prior to」を時間的先行でなく論理的優位と読むと
  		  「…に優位する」。§4 の議論は後者の読みを支持するため後者を採用
  ```

- **F3-4**: 用語集 CSV に登録された訳語に対しては、システムは解釈開示ノートで代替訳を提案しない
- **F3-5**: 解釈フラグの構造化出力がパース不能のとき、システムはフラグを黙って破棄し、翻訳本体はそのまま採用する（翻訳を道連れにしない）

**受け入れ基準**:
- [ ] NST 論文で解釈開示ノートが 1〜5 件生成され、種別ラベル・理由が付いている
- [ ] 6 件以上生成されない
- [ ] 用語集登録語に対する代替提案が出ない
- [ ] フラグのパース失敗を人工的に起こしても翻訳出力は正常に完走する

### F4: UI / CLI

- **F4-1**: Web UI に「深読モード」トグルを 1 つ追加する。3 機能（F1/F2/F3）は一括で有効化され、個別 ON/OFF は提供しない
- **F4-2**: トグル付近に「処理時間と API コストが約 3 割増えます」という注意書きを 1 箇所表示する
- **F4-3**: CLI に `--deep` フラグを追加する。`python3 main.py data/paper.pdf --deep`
- **F4-4**: `--book` と `--deep` が同時指定されたとき、システムは「深読モードは論文モードのみ対応」と警告して深読を無効化し、書籍変換は続行する
- **F4-5**: `server.py` の変換 API は `deep_mode: bool`（デフォルト false）パラメータを受け付ける

**受け入れ基準**:
- [ ] Web UI のトグル 1 つで深読変換が実行できる
- [ ] コスト注意書きが表示される
- [ ] `--deep` で CLI から深読変換が実行できる
- [ ] `--book --deep` で警告が出て書籍変換は通常どおり完走する
- [ ] 深読モード未指定の既存 API 呼び出しは挙動が変わらない（後方互換）

### F5: パイプライン統合

- **F5-1**: F1（問いの構造）と F2（論証地図）は、Phase 3 の後に実行される**独立した深読解析フェーズ**として実装する:
  - ファサード: `core/phase_deep.py`、エンジン: `core/engine/p_deep/`
  - 状態ファイル: `state/<session_id>/phase_deep.json`
  - LLM 呼び出しは**原則 1 回**（入力: 全文＋Phase 3 構造ツリー＋Phase 2 DNA＋expertise、出力: 問いの構造＋要所リストの構造化データ）。コスト目標（§9）を満たせない場合のみ 2 回に分割
  - 使用モデル・ティア処理は既存の `TierManager` / `DEFAULT_MODEL` 系の仕組みに従う
- **F5-2**: `--resume` の番号体系（1〜5）は変更しない。深読有効でフェーズ 4 以降から再開するとき、システムは `phase_deep.json` が存在すればロードし、なければ深読解析のみ再実行する
- **F5-3**: F3（解釈開示）は Phase 4 のプロンプト 2 ルート化で実装する:
  - 通常ルート: 現行 `TRANSLATION_PROMPT` をバイト同一で維持
  - 深読ルート: 通常プロンプト＋解釈フラグ指示の拡張ブロック（`coreprompts.json` に別キー、例 `TRANSLATION_DEEP_ADDENDUM` として管理し、実行時に結合）
  - 解釈フラグは翻訳出力スキーマのオプショナルフィールドとし、欠落してもパースが壊れない設計とする
- **F5-4**: Phase 5 エクスポートは `phase_deep.json` と Phase 4 の解釈フラグを読み、深読有効時のみ F1/F2/F3 のノードを合成する。要所・解釈ノートと訳段落の対応付けは**チャンク ID / TreeNode ID ベース**で行う（テキスト照合に頼らない）
- **F5-5**: 深読解析フェーズが失敗（API エラー・パース不能・タイムアウト）したとき、システムは警告を記録して深読注釈なしの通常出力を完走させる。Web UI には「深読解析に失敗したため通常出力のみ生成しました」と表示する
- **F5-6**: 新規プロンプト（深読解析・翻訳拡張ブロック）は `coreprompts.json` で一元管理し、§2 のスキーマ・F1-4 の根拠ルール・F2-1 の件数制約をプロンプト内に明記する。長文（全文）を投入する深読解析プロンプトは「本文先頭・指示末尾」の long-context 原則（2026-06-07 確立）に従う

**受け入れ基準**:
- [ ] 深読 ON で `state/<session_id>/phase_deep.json` が生成される
- [ ] `--resume 4` で `phase_deep.json` があれば再利用、なければ深読解析のみ再実行される
- [ ] 深読 OFF 時に Phase 4 へ渡るプロンプトが現行とバイト同一である（テストで固定）
- [ ] 深読解析フェーズを強制失敗させても通常出力 2 ファイル（`_p2.md` / `_p2.txt`）が生成される
- [ ] `ronbunnihongo` モード＋深読で `_ronbun.md` に F1/F2/F3 注釈が含まれる

---

## 6. 出力仕様（全体像）

深読モード ON 時の `_p2.txt`（Workflowy 用）の構成。**太字**が追加・変更部分:

```
論文タイトル
- レジュメ
	- **1. 問いの構造**            ← 旧「1. リサーチ・クエスチョン」を置換
		- **民族誌的課題: …**
		- **民族誌的問い: …**
		- **人類学的問い: …**
		- **民族誌的主張: …**
		- **人類学的主張: …**
	- **2. 背後の問いと介入**       ← 旧「2. 核心的主張」を置換
		- **背後の大きな問い: …（根拠: §1 の X 批判、§5 の Y への回帰。本文に明示なし、引用パターンからの推論）**
		- **応答する相手: …**
		- **継承する系譜: …**
		- **ずらしの一手: …**
	- 3. 各セクションの展開          ← 現状維持
- English text                    ← 完全に現状維持（深読の影響なし）
- 日本語本文
- セクションタイトル
	- 訳段落…。**［◆精読］**
		- **⚙️ 論証注釈（要所の説明＋ → セクション参照）**
	- 訳段落…。
		- **⚖️ 分岐: 解釈開示ノート**
	- 訳段落（注釈なし＝流し読み可）…
```

記号の規約: `◆精読`（テンポ信号・インライン）、`⚙️`（論証注釈・子ノード）、`⚖️`（解釈開示・子ノード）。`🌊` 等の装飾絵文字はレジュメ見出しには使わない（既存レジュメの様式に合わせ番号見出しを維持）。

---

## 7. 既存コードとの整合性（変更箇所一覧)

| ファイル | 変更内容 |
|---|---|
| `main.py` | `--deep` フラグ追加、`run_pipeline()` への伝播、`--book` 併用時の警告 |
| `core/pipeline.py` | Phase 3 後の深読解析フェーズ呼び出し（deep フラグ時のみ）、失敗時の継続処理 |
| `core/phase_deep.py` | 新規。深読解析フェーズのファサード |
| `core/engine/p_deep/` | 新規。問いの構造抽出・要所特定のエンジン実装 |
| `core/phase4_translate.py` / `core/engine/p4_translate/prompt_builder.py` | プロンプト 2 ルート化（deep 時のみ拡張ブロック結合）、解釈フラグのパース（オプショナル） |
| `core/phase5_export.py` / `core/engine/p5_export/` | `phase_deep.json`＋解釈フラグの読み込み、F1/F2/F3 ノード合成（ID ベース対応付け） |
| `core/coreprompts.json` | `DEEP_ANALYSIS_PROMPT`・`TRANSLATION_DEEP_ADDENDUM` 追加（既存キーは不変更） |
| `core/models.py` | 深読解析結果のデータモデル追加（問いの構造・要所・解釈フラグ） |
| `server.py` | `deep_mode` パラメータ受付 |
| `web/` | 深読モードトグル＋コスト注意書き |
| `tests/unit/` | F1〜F5 の受け入れ基準に対応するテスト追加。特に「OFF 時バイト同一」の回帰テスト |

**触らないもの**: `core/book_manager.py`（書籍モードはスコープ外）、既存の `TRANSLATION_PROMPT` ほか `coreprompts.json` の全既存キー、English text ブロックの生成ロジック。

---

## 8. エッジケース

- **E-1 純理論論文・レビュー論文**: 対象層なし → 単層縮退＋明示（F1-5）。レビュー論文では「対象」を文献群と見なす読み替えを LLM に許可する
- **E-2 短い論文（本文 10 段落未満）**: 要所 3 箇所以下、解釈ノート 2 件以下に縮小
- **E-3 OCR 品質が低い PDF**: 根拠参照・要所対応がずれるリスク → 対応付けは ID ベース（F5-4）で吸収。深読解析の入力テキスト品質が著しく低い場合も出力はする（品質警告は既存パイプラインの責務）
- **E-4 `[Unlabeled Section]` への参照**: `（→ 冒頭部）` 表記（F2-4）
- **E-5 expertise に専門外・無関係な値**（例: 「料理」）: 分野判定不能として汎用語彙にフォールバック（F1-2）
- **E-6 深読解析の構造化出力が部分的に壊れている**: パースできた部分のみ採用し、壊れた要素は捨てる。全滅時は F5-5 のフォールバック
- **E-7 解釈フラグが上限超過で返ってくる**: エクスポート側で重要度順（LLM 出力順）に 5 件で切り捨て
- **E-8 FREE ティアへのダウンシフト中**: 深読解析も TierManager に従い Lite モデルで実行（品質は落ちるが完走優先）
- **E-9 要所マーク対象の段落が Phase 4 でバッチ分割境界をまたぐ**: 対応付けが ID ベースなら影響しないことをテストで確認

---

## 9. 非機能要求

- **コスト**: 深読モード ON の追加トークン消費・処理時間は通常変換比 **+30% 以内**（測定: NST 論文 PDF を通常／深読で各 3 回実行し、合計トークン数と wall-clock を比較）。これを満たすため深読解析の LLM 呼び出しは原則 1 回（F5-1）
- **後方互換**: 深読モード未指定のとき、全出力（`_p2.md` / `_p2.txt` / `_ronbun.md`）・API レスポンス・状態ファイルは現行と互換（測定: 既存ユニットテスト 165 件が無修正で合格）
- **堅牢性**: 深読解析フェーズの失敗率に関わらず、通常変換の完走率は現行と同一（測定: 深読フェーズ強制失敗のフォールトインジェクションテスト）
- **セキュリティ**: 新規パラメータ（`deep_mode`）はサーバー側でバリデーションする。expertise はプロンプト埋込前に既存のサニタイズ方針に従う

---

## 10. リスクと検証計画

| # | リスク | 検証・対策 |
|---|---|---|
| 1 | **二層問いスキーマの定式が妥当でない**（藤田氏自身が確信を持てていない） | 実装前にプロンプト実験: AL / NST / Techno（人類学）＋非人類学の人文系論文 1 本で「問いの構造」を生成し、藤田氏がレビュー。定式の修正・縮退条件の調整はこの段階で行う。**この実験が本仕様の最初の実装タスク** |
| 2 | **解釈フラグ指示が翻訳品質そのものに干渉する**（藤田氏の懸念。逆に上がる可能性もある） | NST 論文を通常／深読で実行し、`docs/translation_review_checklist.md` で訳文品質を比較（A/B）。劣化が確認されたら F3 を Phase 4 相乗りから独立検証パスに切り替える（設計上の退路） |
| 3 | もっともらしいハルシネーション（背後の問い・介入関係） | 根拠付き推論ルール（F1-4）＋推論ラベルで構造的に抑制。検証 1 のレビューで実際の発生率を確認 |
| 4 | 要所マークの取り違え（違う段落に付く） | ID ベース対応付け（F5-4）＋受け入れ基準のテスト |
| 5 | コスト +30% 超過 | 深読解析 1 呼び出し設計。超過時は入力を全文→セクション要約＋要所候補周辺に絞る縮退案を用意 |

---

## 11. 未解決事項

| # | 事項 | 解決方法 | 担当 | 期限 |
|---|---|---|---|---|
| U-1 | 二層問いスキーマの定式の最終確定（5 スロットの構成・呼称） | §10 検証 1 のプロンプト実験結果をレビューして確定 | 藤田氏 | 実装着手前 |
| U-2 | 非人類学分野での動的語彙の品質（歴史学・文学・哲学でラベルが自然か） | 検証 1 に非人類学論文を 1 本以上含めて確認 | 藤田氏 | 実装着手前 |
| U-3 | 深読解析の使用モデル（DEFAULT_MODEL と同一か、上位モデルか） | 検証 1 で flash / 上位モデルの出力差を比較して決定 | 実装者 | Phase 実装時 |
| U-4 | `◆精読` `⚙️` `⚖️` の記号デザイン最終確定（Workflowy 実機での見え方） | 検証 1 の出力サンプルを Workflowy にインポートして藤田氏が確認 | 藤田氏 | エクスポート実装前 |
| U-5 | 書籍モード対応の設計（章横断の反響・概念追跡） | 論文モードの運用実績を踏まえて別仕様を起こす | — | 未定 |

---

## 12. インタビューで確定した判断の記録

| 論点 | 決定 |
|---|---|
| 拡張の軸 | 読解支援の深化＋品質の新機軸（従来アイデアの再生産は不可） |
| 受益者 | 一般公開ユーザー |
| 提供形態 | 静的アウトライン埋込（対話型・別成果物は不採用） |
| コスト方針 | オプトイン、上限 +30% |
| 説得機構と反響リンク | 「論証地図」として統合（局所と大域の二解像度） |
| 分岐・不可能性・訳注 | 「解釈開示ノート」として統合（種別タグで区別） |
| 推論の扱い | 根拠付き推論のみ出力、推論ラベル必須 |
| レジュメとの関係 | 置換（リサーチ・クエスチョン／核心的主張 → 問いの構造／背後の問い） |
| 注釈の載せ先 | 日本語本文側（インラインマーカー＋子ノード） |
| パイプライン | 独立フェーズ追加＋Phase 4 プロンプト 2 ルート化 |
| 対象モード | 論文モードのみ先行 |
| 命名 | 深読モード / `--deep` |
